import { EntryModalProps } from './EntryModal';
/** useEntryForm + Sidebar. Can also be passed all props of useEntryForm */
declare function EntrySidebar(props: EntryModalProps & {
    className?: string;
}): JSX.Element;
export default EntrySidebar;
